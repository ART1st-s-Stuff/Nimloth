# 2026-06-30 SFT2 1024-dim latent experiment on dgx-56

## 目标

- 将 Nimloth SFT2 的 WM latent/predictor 从 128 维扩大到 1024 维。
- 将 reconstruction decoder 扩大到 1024 hidden、4 层、16 heads，并输出与 VAGEN navigation render 对齐的 255×255 图像，patch_size=15。
- push 分支，并在 superpod 上使用 dgx-56 启动一轮新的 SFT2 实验。
- 根据 dgx-14 / dgx-12 / dgx-56 的 GPU 占用和超时情况选择 RL env server。

## 当前计划

1. 本地提交并 push 配置/代码变更。
2. 连接 superpod，确认已有 hold/占位任务与节点状态。
3. 同步远程 `/project/peilab/atst/nimloth` 到当前 commit。
4. 在服务器 worktree 中启动新的 SFT2 实验，训练节点使用 dgx-56。
5. 选择 dgx-14 或 dgx-12 作为 env server（若确实需要），或记录无需 env server 的原因。
6. 监控到任务健康启动。

## 已完成步骤

- 已阅读 AGENTS、实验规则、server 规则、slurm/on-experiment-start/on-progress skill。
- 已查询 memory，核验了 SFT2 packed-forward 不等价历史记录，确认本实验应保持默认非 packed-forward。
- 已将 WM latent / predictor / reconstruction decoder 配置扩大：
  - `emb_dim=1024`
  - predictor: depth=6, heads=16, hidden_dim=1024, mlp_dim=4096
  - decoder: image_size=255, patch_size=15, hidden_dim=1024, depth=4, heads=16
- 静态验证通过：
  - `python -m py_compile src/nimloth/wm/lewm.py src/nimloth/wm/reconstruction.py src/nimloth/training/sft2/cli.py src/nimloth/training/reconstruction/cli.py`
  - `bash -n experiments/training/reconstruction/train_decoder_sft2_full_dgx12_hold.sh experiments/training/reconstruction/train_decoder_dgx12_hold.sh`
- 已提交并 push：`d554e17 config: scale latent wm and reconstruction` 到 `origin/dev`。

## 文件修改

- `src/nimloth/wm/lewm.py`
- `src/nimloth/wm/reconstruction.py`
- `src/nimloth/training/sft2/cli.py`
- `src/nimloth/training/reconstruction/cli.py`
- `configs/training/sft2/latent_wm_value.yaml`
- `configs/training/reconstruction/defaults.yaml`
- `experiments/training/reconstruction/train_decoder_sft2_full_dgx12_hold.sh`
- `experiments/training/reconstruction/train_decoder_dgx12_hold.sh`

## 数据量估算

- 当前 `train_all.jsonl` 经 `TransitionJsonlDataset` 展开为约 54,702 条 transition。
- 若 `NIMLOTH_DDP_GPU_STRIDE=2`、8 GPU 上实际 4 个模型副本：effective batch = 4×2×4 = 32，1 epoch 约 1,709 optimizer steps。
- 若 stride=1、8 个模型副本：effective batch = 8×2×4 = 64，1 epoch 约 855 optimizer steps。

## 远程启动记录

- 已确认远程 hold/占位任务：
  - dgx-56: hold job `462499`，8×H800 空闲，用于 SFT2 训练。
  - dgx-14: rl-env job `462611`，实际 allocation 暴露 1 GPU 且已有显存占用。
  - dgx-12: rl-env job `462496`，实际 allocation 暴露 1 GPU 且已有显存占用。
- SFT2 是离线训练，不需要 env server；因此未启动 dgx-14/dgx-12 env server。
- 远程 worktree：`/project/peilab/atst/nimloth/.worktree/dev`，commit `c126b88efc28edd19385e12cf796a7675edba5a5`。
- 初次输出目录：`outputs/experiments/training/sft2/2026-07-01/sft2_1024_llmlora_vfull_pair2_ep1_dgx56`。
  - 状态：失败，未进入训练 step。
  - 原因：launcher 虽激活 `.venv-vagen-main`，但实际 `torchrun` 解析到 `.venv`，触发 Qwen2.5-VL `GenerationConfig` 的 `dict.to_dict` 兼容错误。
- 重试输出目录：`outputs/experiments/training/sft2/2026-07-01/sft2_1024_llmlora_vfull_pair2_ep1_dgx56_retry1`。
  - 启动方式：显式使用 `/project/peilab/atst/nimloth/.venv-vagen-main/bin/python3 -m torch.distributed.run`。
  - 状态：已按人类要求手动停止。
  - 健康证据（停止前）：`train_step_log.csv` 曾写到至少 `global_step=21`；GPU 显存约 59–65GB/H800；无 OOM。
  - W&B run id：`ubd9pyyr`。
- 停止记录：
  - 时间：2026-07-01。
  - 原因：人类指出先前代码实现/设计“绝对错误”，要求立即停止当前训练。
  - 操作：kill retry1 launcher 与 dgx-56 hold job `462499` 内的训练子进程；保留 hold job。
  - 停止后检查：dgx-56 8 张 GPU 均为 0 MiB 显存占用。
  - 结论：不得 resume 本 run；已有输出/checkpoint 不能用于结论，需先重新设计/修正实现。

## 待确认/风险

- 当前 1024-dim predictor/StateProjector 实现被人类判定为错误相关路径的一部分；继续训练前必须重新审查并确认正确设计。
- 1024-dim predictor 与 StateProjector 不兼容旧 128-dim checkpoint；若重做实验也必须从合适初始化重新训练 aux modules。
- `.worktree/dev/external/VAGEN` 子模块因 GitHub SSH 权限未能初始化；本次 SFT2 不 import VAGEN，未阻塞启动。
