# 2026-07-19 DINOv2-aligned SFT2

## 任务目标

按人类要求启动一个实验名称明确包含 `dino` 的新 SFT2。由于服务器没有 DINOv3 权重访问且现有 Transformers 不支持 DINOv3，人类明确改为由 AI 选择 DINOv2，并要求使用多节点碎片凑满 8 GPU。

## 人类确认

- Teacher：选择公开 `facebook/dinov2-large`（ViT-L/14，hidden size 1024），与现有 WM state 1024 维直接对齐。
- 资源：多个节点的碎片 GPU，目标 world size 8。
- 实验身份必须明确写 DINO；记录中必须写明实际为 DINOv2，禁止称作 DINOv3。

## 实验前核查

- 目的：在保持正式 k=8 SFT2 其余语义不变时，增加 current RGB observation 的 frozen DINOv2 CLS 对齐，验证 query state 是否获得更强全局视觉语义。
- 代码分支：`feat/dinov3-query-alignment`；DINOv2 泛化改动尚待提交。
- 入口：`experiments/training/sft2/train.py` / `nimloth.training.sft2.trainer`。
- 初始化：`.../full_2e66e97/sft1/epoch_005/hf_merged`，config 与2个safetensors shard存在。
- 数据：`converted_strict_k8_b6c811c/train_all.jsonl` 为3217条、全部 `split=train`、唯一ID3217、success613；`val_all.jsonl` 为355条、全部 `split=val`、唯一ID355、success110。
- cache：复用正式 k8 run 的 compact cache；train 59,389 transitions，val 6,054；k=8/inject/masked/BF16/max_pixels100352，双manifest完整。DINO target读取 `current_image_path`，无需重建Qwen preprocess cache。
- W&B：project `nimloth-sft2`；最大数字ID=29，候选下一个ID=30，提交前必须再次查询避免并发占用。后续发现ID29已于20:25因het component实际8h walltime超时结束，W&B为crashed。
- 资源快照：normal 共15张空闲，单节点最多4；preempt共4张空闲。人类选择跨节点碎片 world size 8。
- 环境：`.venv`=Transformers4.49/PyTorch2.6；`.venv-vagen-main`=Transformers4.55.4/PyTorch2.8。4.55.4原生支持DINOv2；DINOv3不支持且HF cache/token均缺失。
- DINOv2 config已从HF核实：`Dinov2Config hidden_size=1024, patch_size=14`。

## 训练/冻结模块

- Frozen：DINOv2 teacher、Qwen LLM。
- Trainable：Qwen vision encoder（full + EMA）、k=8 query embedding adapter、StateProjector、WM predictor、ValueHead。
- Loss：原 LM CE + WM MSE + SIGReg + value loss，并新增 `lambda_dino=1.0` 的 current-RGB CLS MSE。

## 计划参数

- k=8 / inject / query adapter。
- epochs=10, batch size=2。
- world size=8，grad accumulation=4（保持正式 k8 effective accumulation）。
- max_pixels=100352，max_images_per_batch=12，best按 val WM MSE。
- normal partition，多节点碎片；预计约15–25小时，DINO额外forward的实际开销待健康启动后测量。
- checkpoint：每20分钟 latest、每epoch完整checkpoint、best/final；同一输出目录支持精确resume。
- 监控：total/LM/WM/SIGReg/value/DINO MSE、各组LR、finite检查、GPU显存/利用率、checkpoint完整性、W&B。

## 当前进展

- 已将冻结teacher实现从DINOv3专名泛化为显式DINO-family模型，新增 `latent_wm_value_k8_dinov2.yaml`，保留DINOv3 config供未来使用。
- targeted tests 25 passed；相关suite 76 passed；compileall/diff check通过。

## 当前阻塞

- VPN已恢复。已核查ID29的heterogeneous Slurm与multi-node `srun`启动方案；它可建立跨节点NCCL，但脚本使用物理8GPU配对为world4，且第二het component实际只有8h并导致任务超时。我们的world8启动必须一GPU一rank，并确保每个het component walltime一致且足够。
- 已创建远程detached worktree `/project/peilab/atst/nimloth/.worktree/dino-query-align`，公开 `facebook/dinov2-large` 已缓存并在Transformers4.55.4完成CPU加载检查（hidden1024/patch14/BF16 finite）。
- 已保留W&B ID30 / run `c2lkxd63`，正式名称 `30_dino2cls_k8inject_all3217_qadapter_vfull_wmtrain_l1_ep10_b2_ga4_ws8_px100352_img12_bestwm`。
- 首次Slurm尝试 `480794-480797` 在四个2GPU碎片上分配成功，但因runner首行调用不可用且被静默的`module`函数，四个component均在0秒以127退出，Python/模型/W&B训练均未启动。
- 第二次held提交 `480798-480801` 未释放/未占卡：确认站点submit plugin会把het component1–3强制重置为默认8h，并禁止通过`scontrol`延长；trap已安全取消。因资源布局是均匀2+2+2+2，无需heterogeneous job，启动方案改为单个常规4-node batch（每节点2GPU）。
- 第三次held提交 `480805` 正确获得常规4-node/总8GPU/48h请求，但launcher把Slurm `NumNodes=4-4`和含多个`=`的ReqTRES误解析为失败，按设计在release前安全取消。
- 第四次held提交 `480806` 已通过字段验证，但站点plugin同样禁止普通用户用`scontrol release`（提示缺少仅对`srun`有意义的account参数）；ERR trap再次在未release/未占卡状态安全取消。常规batch的48h请求已由test-only与held job两次证实，最终改为直接提交后立即只读验证，异常则scancel。
- 第五次/当前正式作业 `480807` 已健康运行：单个regular job在`dgx-21,dgx-28,dgx-29,dgx-54`用4 nodes×2 H800组成world8，总64CPU/384G/48h。runtime24m41s时CSV到step277且所有loss finite（DINOv2 CLS MSE1.49439）；8卡99–100%利用率、约61.6–64.3GiB、35–61C；W&B `c2lkxd63` running/heartbeat正常；3.78GB periodic latest checkpoint可读（epoch1/step263/micro1052）。日志无traceback/OOM/NaN/NCCL error/timeout；存在mixed IB/RoCE与DDP grad-stride非致命性能warning，需后续观察。

## 后续性能优化（不打断当前作业）

- 人类选择“继续当前，优化后续”。当前运行commit保持`5e7fc3f`，不得把开发分支新代码同步进正在运行的远程worktree。
- SFT1真实对比：613条success-only记录、5 epochs、仅50 optimizer steps，15:32:46–15:59:58共27m12s；当前SFT2为59,389 transitions、10 epochs、每epoch1456步/总14,560步。当前单步约4.57s，反而快于SFT1约17–18s；无法在保持数据/目标不变时把总walltime降到SFT1半小时级。
- 已实现未来run用的DINO CLS sidecar：基于现有compact cache的train/val image index，CUDA BF16 teacher输出以float32分片存储；manifest校验teacher source/revision、processor、父cache、图片stat/index、hidden/dtype/shard，并支持partial build恢复。训练可用`--dino-cache-dir --require-dino-cache` fail closed，checkpoint invariant记录cache fingerprint，禁止在线teacher fallback。
- 新配置：`configs/training/sft2/latent_wm_value_k8_dinov2_cached.yaml`；构建入口：`experiments/training/sft2/build_dino_feature_cache.py`。CPU fake-cache/CLI/loss/resume相关suite 81 passed，compileall/diff-check通过。
- 人类已确认1-H800 cache验收。Slurm `480899` 在dgx-10用15m18s exit0完成：cache306MB，train59,389+val6,054 rows，revision/processor/父cache/图片/shape/dtype/finite均通过，16个online/cache样本bitwise一致，fingerprint `7568578e15909872`。
- Teacher-only benchmark online→cache：batch1 68.1→17,792 img/s（261x），batch2 85.7→19,458（227x），batch4 96.8→20,236（209x），batch8 104.3→21,171（203x），batch16 107.8→21,452（199x）。但当前sampler仅约1.27张DINO图/micro（max4），GA4推算teacher约占4.57s step中的0.06s/~1.3%，整run只省约15分钟；这是组件推算而非whole-SFT2 A/B。
- 结论：DINO cache正确可复用，但不是小时级瓶颈。packed trajectory forward已证实多图语义不等价，禁止正式使用。
- 人类确认world8精确profile。Job`480933`在4节点×2卡用14m24s exit0完成3×50步、无checkpoint/W&B：epoch1模式下online timing为backward+DDP3.010s(64%)、next Qwen0.930s(20%)、current Qwen0.661s(14%)、DINO0.090s(1.9%)，其余约0.05s；cached DINO降至0.0078s。主瓶颈明确为backward/跨节点每micro同步，其次双Qwen forward。
- Profile同时发现真实bug：Transformers `from_pretrained`默认eval，而trainer在首轮前没有`model.train()`；`evaluate()`只在epoch1 val后恢复train。因此profile中所谓GC实际未激活，`cached_no_gc`对照无效；正式job480807 epoch1同样eval，epoch2起会切train并激活checkpointing。Qwen config attention_dropout=0，主要即时风险是性能/显存阶段变化，但仍须监控。
- 已修复未来代码：首轮前显式将Qwen/state_proj/WM/value设train，并记录requested/active checkpointing；相关suite82 passed。必须重跑bounded world8 profile后才能选择checkpointing策略，禁止使用480933的无GC结论。
