collect:
  num_episodes_per_scene: 50
  max_steps_per_episode: 50
  num_workers: 4
  image_height: 518
  image_width: 518
  # 顺序对应 ACTION_SPACE:
  # [MoveAhead, RotateLeft, RotateRight, LookUp, LookDown, Done]
  action_weights: [6.0, 1.0, 1.0, 0.5, 0.5, 0.1]
  operation:
    outputs_root: datasets
    phase: phase1
    task: wm_data_collection
    resume: true
