defaults:
  - pipeline/collect: default
  - pipeline/train: default
  - pipeline/calib: default
  - pipeline/rollout: default
  - dataset: ai2thor
  - wm: cfm
  - pm: none
  - vlm: none
  - _self_

project:
  name: flower
  seed: 114514

hydra:
  run:
    dir: outputs/hydra/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: outputs/hydra/multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}

