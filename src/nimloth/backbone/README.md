# Backbone (`nimloth.backbone`)

模型骨干网络相关工具，供训练与推理复用，不绑定某个 training phase。

| 子目录 | 内容 |
|--------|------|
| `qwen25vl/` | Qwen2.5-VL batching、latent extraction、tuning、vision EMA 与窄范围诊断 patch |

训练编排（loop、loss、schedules）仍在 `nimloth.training`。
