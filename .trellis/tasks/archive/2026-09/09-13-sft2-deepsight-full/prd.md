# Stage2 DeepSight full tuning comparison
User authorized implementation and remote comparison, then task creation. Start from the same Stage1 epoch2 input as previous Stage2 on a100-1. Freeze Qwen vision encoder and original visual merger; fully train language transformer, complete embedding and LM head, and existing DINO alignment projector. DINO teacher remains fixed. Use 2e-5 LR and 10% warmup. Retain current K64/grid8, trajectory objectives and splits. Preserve all prior artifacts. Acceptance: explicit supported mode; tested freeze and optimizer boundaries; save/resume support; real 8-GPU run reaches optimizer steps with finite metrics. Retain convergence policy and every10-step checkpoints from user instructions. This is adaptation of DeepSight tuning scope, not a reproduction of its driving dataset or architecture.

Follow-up comparison: from the same Stage1 epoch2 initialization, use DINO weight4 on a100-1 eight GPUs and raise only the64 Query token rows to1e-4 (5x the2e-5 base LR). In input embedding and independent LM head, train only Query rows, eight action rows, and action_start/action_end; freeze EOS and every other vocabulary row. Continue full training of the language transformer and projector at2e-5; keep the complete visual stack frozen. Stop after a fully validated and committed epoch5.
