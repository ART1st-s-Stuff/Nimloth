# Design
Base commit 99207d3518ddba896efd803031d8954141197291; new worktree .worktree/sft2-deepsight-full, branch codex/sft2-deepsight-full. Add explicit Stage2 full-language mode without changing legacy selective LoRA defaults. Bypass selected-row wrappers for this mode, freeze complete visual module, preserve FP32 embedding masters and BF16 forward. Include tuning mode in checkpoint identity. Dense export must remain dense and restore optimizer groups correctly. Keep existing projector/objective rather than replace with DeepSight architecture. All trainable groups LR2e-5; maintain existing FSDP for 8 A100 resources and convergence schedule with warmup0.1. DeepSight source config/ad_bev_v3.yaml uses cosine/one epoch; existing user convergence policy remains, so explicitly disclose schedule adaptation. Inspect full trainable dtype and FSDP memory before launch.

The Query-5x follow-up retains the full language transformer but replaces dense vocabulary-table updates with separately materialized FP32 Query and protocol rows. Query rows use1e-4; the protocol subset is exactly eight actions plus action_start/action_end at2e-5. Dense embedding/head tensors are frozen, including EOS and ordinary vocabulary. Export materializes selected rows back into standard dense tensors; resume reinstalls the selected-row optimizer topology from the dense artifact and checkpoint identity.
