# 548155重提：环境runtime路径修正

## 授权与范围
人类于2026-09-06明确要求重提548155。保持原compatibility smoke：base seed27670、step60 actor、1条轨迹、20 turns、最多512 token/turn、greedy、四环境GPU加四policy GPU/TP1、preempt一节点8GPU/224CPU/180G/8h、排除dgx-51。此次不扩大到batch1正式收集，不启动SFT训练。

## 原失败
548155在dgx-33运行7:43后FAILED(1:0)。环境创建HTTP500：NavigationEnvConfig不接受example_count。客户端使用重建VAGEN170a673d，服务端却由主仓external/VAGEN加载。AI2-THOR smoke和模型加载已走通；没有完整trajectory数据，不能断点恢复。旧root保持不动。

## 修复
基于原commit69a0cef6建立codex/step60-retry-548155专用分支，只修环境launcher的VAGEN_DIR/PYTHONPATH/解释器选择，并验证真实配置构造。保留example_count等原语义，不删除字段或放宽validator。其余SFT实现分支不参与此次提交。

## 精确运行绑定
远程根：/project/peilab/atst/nimloth。
Nimloth远程新worktree：.worktree/step60-retry-548155，绑定修复后的commit。
VAGEN：.worktree/vagen-step60-runtime-reconstruction-vagen，170a673d1bf5855fc0ea6fbed0744b3d7168f8f0。
解释器：/project/peilab/atst/nimloth/.venv/bin/python3。
actor：outputs/experiments/training/sft1-vagen-step60/20260904T181500Z_step60_batch1_v3_venv_r8_7dac687b/merge/hf_actor。
新输出：outputs/experiments/training/sft1-vagen-step60/20260906T060000Z_retry548155_runtime_fix。
训练被冻结/不执行，trainer.val_only=True；此兼容smoke不等于最终terminal-CoT转换数据。

## 提交前验证
刷新sacct/squeue与资源；本地shell语法及路径回归；远程精确commit/干净worktree、真实VAGEN config构造与服务导入、固定模型文件、原输入行base27670、脚本下游阶段与输出未占用。提交时仅export声明的运行变量；不得输出.env内容。

## 监控
只提交一次，不自动失败重试。记录返回job ID和UTC提交时刻，直接检查squeue/sacct和新stdout/子日志。保持原8h运行上限；出现失败使用end skill保留证据。若排队导致本会话交接，保存在本Trellis任务progress.md的精确job与查询命令，不声称执行完成。

## 用户授权两卡替换（2026-09-06）
用户要求查看ETA，排队不理想时可换2卡先跑。06:26Z查询550810仍PENDING，8卡预计2026-09-08T10:18:14Z（约52小时）。normal两卡调度预测约2026-09-07T04:00Z；normal_debug_qos也没有更早保证。当前dgx-35有2GPU/68CPU空闲。
计划替换550810而非并行重复运行：normal/normal_debug_qos，一节点2GPU、56CPU、180G、2h，无重排重试，exclude dgx-51，不固定节点。环境1GPU、策略1GPU/world_size1/TP1，policy CPU28；单条base seed27670、step60 actor、20turn/512token、greedy、不训练均保持。两卡结果只证明此拓扑，不能证明四policy worker源拓扑。输出新建20260906T063500Z_step60_2gpu。
最小修改现有orchestrator支持TOTAL_GPU_COUNT=2，并隔离共享节点Ray进程/端口/temp，禁止用户范围pkill或ray stop。提交前独立审核及CPU测试；远程精确新commit、干净工作区、runtime与actor保持前次绑定。替换前再次查询550810，如已开始则保留正在运行作业并重新评估。
命令覆盖：sbatch --job-name=vagen-step60-2g --partition=normal --qos=normal_debug_qos --gres=gpu:2 --cpus-per-task=56 --mem=180G --time=02:00:00 --no-requeue --exclude=dgx-51 --export=ALL experiments/training/sft1/rollout_full_6gpu_preempt.slurm。export TOTAL_GPU_COUNT=2 ENV_GPU_COUNT=1，其余原数据和解释器绑定不变，运行名step60_2gpu。端口在分配节点启动前检查，不保证预测资源持续可用。

两卡使用独立远程worktree `/project/peilab/atst/nimloth/.worktree/step60-two-gpu`；export PORT_BASE=18060，启动时核验占用与自身server PID。原550810源码目录不原地修改。

## 自动重试与hold550857
用户已授权修复后自动重提直到健康实际生成，并要求保留allocation。550857在dgx-05保留2GPU/56CPU/180G，normal_debug_qos，2h；不自动增加hold时限。子实验以srun --jobid运行，失败不终止hold，stdout/exitcode均写新attempt root。
实际根因：venv editable verl指向主仓external/VAGEN/verl commit138a1d17，vllm_rollout_spmd按model_path包含Qwen2.5-VL才传limit_mm_per_prompt。actor hf_actor绕过该分支，尽管真实VAGEN yaml已有limit20。修复按model_hf_config.model_type识别，不改history(window5=当前加过去5图)或原limit20；显式VERL_DIR选择独立已提交runtime。原remote registry.py兼容补丁原样复制并提交到隔离runtime，共享原文件不改。
Nimloth branch codex/step60-retry-548155，verl branch codex/step60-mm-limit。远程分别新worktree step60-mm-retry 与 verl-step60-mm-limit。原actor/数据/greedy/TP1/val_only不变；attempt root20260906T065000Z_step60_mmfix_a1，每失败保留证据后修复+preflight再同hold新目录重启。健康标准为环境reset成功、至少多步真实生成成功且未发生多图/进程错误，优先观察完整单条轨迹。
