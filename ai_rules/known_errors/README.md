# Known Errors

本目录记录已经明确发生过、且值得后续 agent 避免重复的错误模式。

用途：
- 不是进度日志；
- 不是 memory 的替代；
- 而是项目内已知的、可复查的 agent 失误清单。

编写要求：
- 每个错误单独一个 markdown 文件；
- 只记录已经实际发生过的错误；
- 说明错误结论、错误原因、正确做法；
- 尽量给出对应文件/代码位置作为依据；
- 不要写成长篇复盘。

建议命名：
- `E0001_<short_name>.md`
- `E0002_<short_name>.md`
