# Deprecated — moved to `experiments/training/sft2/`

SFT2 训练入口、Slurm 与 submit 脚本已迁至：

- `experiments/training/sft2/train.py`
- `experiments/training/sft2/train_vagen79_default.slurm`
- `experiments/training/sft2/submit_*.sh`

`train_sft2_qwen25vl.py` 保留为薄 wrapper，转发至新路径。
