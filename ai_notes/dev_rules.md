# 开发规则笔记

- 重要性: high
- L: 3
- D: 2

## 行为约束

- 除单元测试外，不允许自动回退到 mock。
- 在当前项目场景下，真实采集（如 AI2THOR）不可用时必须显式报错。
- 所有与预期不符的情况都应提供明显错误，不使用静默降级或自动回退。
- 所有推理默认走 CUDA；若 CUDA 不可用，必须立即报错，禁止回退到 CPU 推理。
- 所有脚本默认提供可见进度条（至少覆盖主要耗时阶段），避免长时任务无反馈。

